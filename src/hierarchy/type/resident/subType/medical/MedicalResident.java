package hierarchy.type.resident.subType.medical;

import hierarchy.type.resident.Resident;


public abstract class MedicalResident extends Resident {

    public MedicalResident(String name, String jobTitle) {
        super(name, jobTitle);
    }
}