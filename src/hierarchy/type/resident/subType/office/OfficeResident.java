package hierarchy.type.resident.subType.office;

import hierarchy.type.resident.Resident;

public abstract class OfficeResident extends Resident {

    public OfficeResident(String name, String jobTitle) {
        super(name, jobTitle);
    }
}